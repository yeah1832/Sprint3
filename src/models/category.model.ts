export interface Category{
    category_id: number | null;
    category_name: string;
    isActive?: boolean;
}

export interface Category2{
    category_id: number | null;
    category_name: string;
}